----------------------------------------------------------------------------Sinus számolása

#include <iostream>
#include <stdio.h>
#include <math.h>

#define PI 3.14159265

using namespace std;

int main ()
{
  double x, sinx;
  cout<<"x= ";
  cin>>x;

  sinx = sin (x*PI/180);

  cout<<"The sine of "<< x << " degrees is "<< sinx;
  return 0;
}

//-----------------------------------------------------------------------polinom beolvasas szorzas
void polinom()
{
int n;
cout << "hanyadfoku a polinom? n= ";
    cin>> n;
vector<int> P(n+1);
vector<int> Q(2);
vector<int> R((P.size()+Q.size())-1);

cout<<"Add meg a P polinom ertekeit!"<<endl;
for(int i=0;i<P.size();i++)
{
 cout<< i<<" foku tag:";
  cin>> P[i];
};

cout<<"Add meg a Q polinom ertekeit! (elsofoku)"<<endl;
for(int i=0;i<Q.size();i++)
{
 cout<< i<<" foku tag:";
  cin>> Q[i];
};

for(int i=0;i<R.size();i++)
{
  R[i]=0;
};

for(int i=0;i<P.size();i++)
{
 for(int j=0;j<Q.size();j++)
  {
    R[i+j]+=P[i]*Q[j];
  };
};

 cout<< "A Polinom: "<<endl;
for(int i=0;i<R.size();i++)
{
  cout<< R[i] <<" x^"<<i<<"  ";
};

};
--------------------------------------------------------------------------polinom beolvasas szorzas vege

//------------------------------------------------------fuggveny ertek adasa
void fuggveny(double x)
{
    cout << "x= ";
    cin>> x;
    cout <<"f(x)=  "<< f(x) <<endl;
};
double f(double x)
{
  double sinx,y;

  sinx = sin (x*PI/180);
  y=(-1)*exp((x/2)*sinx);
 return y;
};
//-------------------------------------------------------fuggveny ertek vissza adas vege


---------------------------------------------------------------------------feladat
feladat: f(x) = -e^(x/2)sin(x)
n=2...100 pontokban minden n-re meg kell adni a polinomot.

----------------------------------------------------------------------------Mátrix
Mátrix PL:
[1 f(1)
[2 f(2) (f(2)-f(1))/(2-1)

2pont: 2x3

1 f(1)
2 f(2)(f(2)-f(1))/(2-1)
3 f(3)(f(3)-f(2))/(3-2) *

*(f(3)-f(2))/(3-2) -(f(2)-f(1))/(2-1)) /(f(3)-f(1))

legyen nxn es Mátrixunk, ami alsó hármoszög.
és legy egy n elemü vektorunk a pontoknak.
i, sor j-edik oszlop.
---------------------------------------------------LÉnyeg
Ötlet:
Legyen:
nXn es mátrixunk (100x100as): m[100][100]
100 elemü n ünk(amibe a pontokat tároljuk): n[100]
f(x) = -e^(x/2)sin(x)

Interpolációval megadjuk a polinómot.

ekkor a mátrix elsõ sora(j=0)

i=1, i<n
m[i][0]=f(n(i))
i++;

a mátrix következõ soraira //i=j mert alsõ háromszög m.
j=1
{
	i=j, i<n
	m[i][j] := (m[i][j-1]-m[i-1][j-1])/(v[i]-v[i-j]);      i++;
}
j++

Ekkor a n=100-ra a polinóm:

i=0;
m[i][i]*szorzat(i)

szorzat(i):

string sz;
k=1; k<(i+1)
sz=sz ++ "(x-v[k-1])"
k++;


hogyan számolja nekem ki (x-v[k-1])...(x-v[k-i])?
P(2) = [1,]
---------------------------------------------------------------------------

